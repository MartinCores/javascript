//Completa la función para que retorne el último elemento del array
arrayApps = [1, 2, 3, "catorse"];

function getLastApp(apps) {
  return apps[apps.length - 1];
}

console.log(getLastApp(arrayApps));
