//Completa la función para que retorne el número de elementos que contiene el array que recibimos cómo
//parámetro
function getNumberOfElements() {
  const elements = [0, 1, 2, 3];
  return elements.length; //4
}
console.log(getNumberOfElements());
