//Completa la función sayHello de modo que retorne el nombre completo usando la interpolación de variables
const firstName = `Martinho`;
const lastName = `Cores`;

const sayHello = () => {
  return firstName + ` ` + lastname;
};

console.log(`Chámome ${firstName} ${lastName}`);
