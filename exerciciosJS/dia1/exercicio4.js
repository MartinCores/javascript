//Completa la función getFirstCharacter de modo que retorne el primer carácter del parámetro name que recibe
const name = "cousa";

function getFirstCharacter(name) {
  return name[0];
}

console.log(getFirstCharacter(name));
