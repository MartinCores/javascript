//Completa la función skipFirstCharacter de modo que retorne todos los caracteres exceptuando el primero
const name = "Wese";

function skipFirstCharacter(name) {
  return name.substring(1);
}
console.log(skipFirstCharacter(name));
